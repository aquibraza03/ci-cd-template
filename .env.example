# =====================================================
# AWS CONFIGURATION (ap-south-1 / Mumbai)
# =====================================================
# RECOMMENDED: Use IAM Role with GitHub OIDC
# Do NOT commit real credentials to git

AWS_REGION=ap-south-1
AWS_ACCOUNT_ID=123456789012

# Preferred authentication (recommended)
AWS_ROLE_ARN=arn:aws:iam::123456789012:role/github-actions-role

# Fallback (NOT recommended – use only for local testing)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# =====================================================
# SECURITY SCANNING (Optional but recommended)
# =====================================================

SNYK_TOKEN=your-snyk-token-here
TRIVY_CACHE_DIR=/tmp/trivy

# =====================================================
# NOTIFICATIONS (Optional)
# =====================================================

N8N_WEBHOOK_URL=https://your-n8n-instance/webhook/deploy
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# =====================================================
# APPLICATION SETTINGS
# =====================================================

APP_NAME=my-sme-app
APP_ENV=production
APP_PORT=3000

# =====================================================
# DATABASE (Optional)
# =====================================================
# Never commit real database credentials

DB_HOST=your-rds-endpoint.ap-south-1.rds.amazonaws.com
DB_NAME=myapp_db
DB_USER=admin
DB_PASSWORD=your-db-password

# =====================================================
# CLIENT SETUP INSTRUCTIONS
# =====================================================
# 1. Add these variables to GitHub Secrets (NOT .env files)
# 2. Follow docs/secrets-guide.md
# 3. Run: ./scripts/setup.sh
# 4. Push code: git push origin main

